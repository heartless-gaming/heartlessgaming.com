/**
 * should be type with typescript as 'STANDARD' 'PRINTFUL_FAST'
 * and the other return value of printful api
 */
export function useStatePickedShippingRates() {
  return useState('pickedShippingRates', () => '')
}
